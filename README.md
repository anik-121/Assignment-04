1. Difference:
    getElementById("id"):
    a) Use Id
    b) select one element

    getElementsByClassName("class"):
    a) Use class name
    b) Select multiple elements

    querySelector("selector"):
    a) Use any CSS selector
    b) Select first matching element

    querySelectorAll("selector"):
    a) Use any CSS selector
    b) Select all matching elements


    2. createElement() then add content and append()

    3. Event starts from the target element

    4. Add event listener to parent

    5. Difference:

    preventDefault()
    a) Stops default browser behavior

    stopPropagation()
    a)Stops event from bubbling up